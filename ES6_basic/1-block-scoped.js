export default function taskBlock(trueOrFalse) {
  var task = false;
  var task2 = true;

  if (triueOrFalse) {
    var updateTask = true;
    var updateTask2 = false;
	  return [updatedTask, updatedTask2];
  }

  return [task, task2];
}
